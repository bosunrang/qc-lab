{
  // Build phần `renderer/browser-mock/*` chạy được ngoài trình duyệt sang
  // CommonJS để test bằng Node — hiện chỉ còn `sqlite-shim.ts` (lớp bọc
  // sql.js, xem app-v2/tests/sqlite-shim.test.mjs). Cần lượt build RIÊNG vì
  // tsconfig.app-v2-renderer.json phát ESM+JSX cho Vite, còn
  // tsconfig.app-v2-main.json chỉ include app-v2/main.
  //
  // `api.ts`/`store.ts`/`permission-policy.ts` (bản giả lập viết tay) đã bị
  // xoá 2026-09-09: bản xem trước giờ chạy CHÍNH handler thật — xem
  // renderer/browser-mock/real-api.ts.
  "compilerOptions": {
    "target": "es2022",
    "lib": ["es2022", "dom"],
    "module": "commonjs",
    "strict": true,
    "noEmitOnError": true,
    "rootDir": "app-v2",
    "outDir": "app-v2-dist/mock",
    "skipLibCheck": true,
    "esModuleInterop": true,
    "types": ["node"]
  },
  "include": ["app-v2/renderer/browser-mock/sqlite-shim.ts"]
}
