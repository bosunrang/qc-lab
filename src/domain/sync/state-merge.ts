export function createSyncStateMerge(deps:any){return(local:any,remote:any,base:any)=>{const out=deps.clone(remote),ls=deps.snap(local),bs=deps.snap(base).keys;deps.top.forEach((k:string)=>{if(deps.lists.has(k))out[k]=deps.array(local[k],remote[k],(base||{})[k],true);else if(ls.keys[k]!==bs[k])out[k]=deps.cloud(local[k])});out.data=deps.branch(local,remote,base,'data');out.sigmaData=deps.branch(local,remote,base,'sigmaData');return out}}
export function uniqueSyncUsers(users:any[]){const seen=new Set<string>();return(users||[]).filter(u=>{const k=String(u?.username||'').toLowerCase();if(!k)return true;if(seen.has(k))return false;seen.add(k);return true})}
