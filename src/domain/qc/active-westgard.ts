export function createActiveWestgard(deps:any){return(views:any[],within:Set<string>,across:Set<string>,verdict:(r:string[])=>string)=>{const rows=views.map(v=>({...v,single:v.pts.length?deps.single(v.pts,v.l.mean,v.l.sd,(r:string)=>within.has(r)):{F:[],zs:[]}})),cross=deps.multi(rows.map(v=>({level:v.l.level,pts:v.pts,mean:v.l.mean,sd:v.l.sd})),(r:string)=>across.has(r)),byPoint=new Map();rows.forEach(v=>v.pts.forEach((p:any,i:number)=>{const one=v.single.F[i]||{},rules=[...new Set([...(one.rules||[]),...(cross.get(p)||[])])],supportRules=[...new Set([...(one.supportRules||[]),...((cross.support?.get(p))||[])])].filter((r:string)=>!rules.includes(r));byPoint.set(p.id,{level:verdict(rules),rules,supportRules,z:v.single.zs[i]})}));return{views:rows,cross,byPoint};};}
