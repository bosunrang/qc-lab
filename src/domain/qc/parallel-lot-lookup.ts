export function createParallelLotLookup(deps:any){return(test:any,level:any)=>{const config=deps.level(test,+level);if(!config||!config.qcLotId)return null;const panel=deps.panel(test);if(!panel)return null;const transition=(deps.transitions()||[]).find((item:any)=>item&&item.status==='active'&&item.panelId===panel.id&&item.fromLotId===config.qcLotId);if(!transition)return null;const lot=(deps.lots()||[]).find((item:any)=>item.id===transition.toLotId);if(!lot||+lot.level!==+config.level)return null;const target=deps.target(test,config.level,lot.id,lot.lotNo);if(!target||!Number.isFinite(+target.mean)||!(+target.sd>0))return null;return{tr:transition,lot,lotNo:lot.lotNo||'',mean:+target.mean,sd:+target.sd,low:target.low,high:target.high,exp:lot.exp||''}}}
