export function previousLotHistory(level:any,lineage:any[],meanSd:any,points:any){if(!level)return[];return lineage.filter(x=>(x.lotNo||'')!==(level.lot||'')).map(x=>{const m=meanSd(x.lotNo),p=points(x.lotNo);return m&&p.length?{lot:x.lotNo,mean:m.mean,sd:m.sd,pts:p}:null}).filter(Boolean);}
export function lotGroupLevels(group:any,tests:any[],lots:Map<any,any>){const ids=new Set(group?.lotIds||[]),out:any[]=[];tests.forEach(t=>(t.levels||[]).forEach((l:any)=>{let lot:any,mean=NaN,sd=NaN;if(ids.has(l.qcLotId)){lot=lots.get(l.qcLotId);mean=+l.mean;sd=+l.sd}else{const h=(l.meanSdHistory||[]).slice().reverse().find((x:any)=>ids.has(x.qcLotId));if(h){lot=lots.get(h.qcLotId);mean=+h.mean;sd=+h.sd}}if(lot&&Number.isFinite(mean)&&Number.isFinite(sd)&&sd>0)out.push({t,l,lot,mean,sd})}));return out.sort((a,b)=>String(a.t.name||'').localeCompare(String(b.t.name||''),'vi')||a.l.level-b.l.level);}
