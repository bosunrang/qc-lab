export function rangeSafetyGate(nce:any,tea:any,causeConfirmed:unknown,bias:any){if(!nce)return{needed:false,threshold:null,passes:true};const teaValue=Number(tea),biasValue=Number(bias),threshold=Number.isFinite(teaValue)&&teaValue>0?teaValue/4:null;return{needed:true,threshold,passes:!!causeConfirmed&&threshold!=null&&Number.isFinite(biasValue)&&Math.abs(biasValue)<=threshold};}
