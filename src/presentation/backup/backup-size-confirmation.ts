export function createBackupSizeConfirmation(deps:{error:(size:any)=>string;size:(value:any)=>string}){return(input:{bytes:any;title:string;detail:string})=>{const error=deps.error(input.bytes);return error?{kicker:'Vượt giới hạn khuyến nghị',title:input.title,message:`${error} Dung lượng thực tế ${deps.size(input.bytes)} MB.`,detail:input.detail,confirmLabel:'Vẫn tiếp tục',cancelLabel:'Hủy'}:null;};}
