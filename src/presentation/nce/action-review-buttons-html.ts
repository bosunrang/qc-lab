type ButtonModel = { edit?: boolean; escalate?: boolean; approve?: boolean; returnForRevision?: boolean; reopen?: boolean; cancel?: boolean };

export function createActionReviewButtonsHtml(deps: { button: (label: string, action: string | { action: string; args?: unknown[] } | null, variant: string, title?: string) => string }) {
  return (index: number, model: ButtonModel) => `<div class="action-row-actions">${deps.button('Chi tiết', { action: 'viewActionDetail', args: [index] }, 'ghost sm')}${model.edit ? deps.button('Tiếp tục', { action: 'editAction', args: [index] }, 'ghost sm') : ''}${model.escalate ? deps.button('Lập hồ sơ tiếp theo', { action: 'escalateAction', args: [index] }, 'teal sm', 'Hành động chưa hiệu lực — mở vòng điều tra mới') : ''}${model.approve ? deps.button('Duyệt', { action: 'approveAction', args: [index] }, 'teal sm') : ''}${model.returnForRevision ? deps.button('Trả lại', { action: 'returnAction', args: [index] }, 'ghost sm') : ''}${model.reopen ? deps.button('Mở lại', { action: 'reopenAction', args: [index] }, 'danger sm', 'Hồ sơ đã duyệt nhưng không còn đủ điều kiện khép vòng') : ''}${model.cancel ? deps.button('Hủy hồ sơ', { action: 'cancelAction', args: [index] }, 'danger sm', 'Hủy có lưu vết — không xóa dữ liệu') : ''}</div>`;
}
