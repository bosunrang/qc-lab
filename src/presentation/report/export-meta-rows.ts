export function createExportMetaRows(deps:any){return(kind='Báo cáo')=>{const app=deps.app()||{version:'dev'},rules=Object.entries(deps.rules()||{}).filter((entry:any)=>entry[1]!==false).map((entry:any)=>entry[0]).join(', ');return[['Metadata',kind],['Phiên bản app',`${app.name||'QC Lab'} ${app.version||'dev'}`],['Người xuất',deps.userName()],['Thời gian xuất',deps.formatDateTime(deps.now())],['Bộ luật áp dụng',rules||'Chưa cấu hình']];};}
