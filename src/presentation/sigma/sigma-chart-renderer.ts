export function createSigmaChartRenderer(deps:{levels:(row:any)=>any[];canvas:(width:number,height:number,scale:number)=>any;font:(weight:string,token:string,fallback:number)=>string;zone:(sigma:number)=>{c:string};bytes:(url:string)=>Uint8Array}){return(rows:any[])=>{const data=rows.map(row=>({name:row.period||row.name,levels:deps.levels(row).filter(item=>item.metric.classifiable!==false&&Number.isFinite(item.metric.sigma))})).filter(row=>row.levels.length);if(!data.length)return null;const count=data.length,width=Math.max(760,72*count+170),height=400,scale=6,left=52,right=22,top=54,bottom=92,innerWidth=width-left-right,innerHeight=height-top-bottom,all=data.flatMap(row=>row.levels.map(level=>level.metric.sigma)),maximum=Math.max(8,Math.ceil(Math.max(...all)*1.05)),canvas=deps.canvas(width,height,scale),ctx=canvas.ctx,y=(value:number)=>top+innerHeight-(value/maximum)*innerHeight;([[0,3,'#f6dcd8'],[3,4,'#fdeecb'],[4,6,'#e4eee3'],[6,maximum,'#d6e8de']] as [number,number,string][]).forEach(band=>{if(band[1]>band[0]){ctx.fillStyle=band[2];const y1=y(Math.min(band[1],maximum)),y2=y(band[0]);ctx.fillRect(left,y1,innerWidth,y2-y1);}});ctx.font=deps.font('','type-meta',12.5);ctx.fillStyle='#9a9486';ctx.textAlign='right';ctx.strokeStyle='rgba(0,0,0,.06)';ctx.lineWidth=1;for(let grid=0;grid<=maximum;grid+=2){const position=y(grid);ctx.beginPath();ctx.moveTo(left,position);ctx.lineTo(width-right,position);ctx.stroke();ctx.fillText(String(grid),left-6,position+4);}const reference=(value:number,color:string)=>{ctx.save();ctx.strokeStyle=color;ctx.setLineDash([5,4]);ctx.lineWidth=1.3;const position=y(value);ctx.beginPath();ctx.moveTo(left,position);ctx.lineTo(width-right,position);ctx.stroke();ctx.restore();ctx.fillStyle=color;ctx.textAlign='left';ctx.font=deps.font('bold','type-meta',12.5);ctx.fillText(value+'σ',width-right-26,position-4);};reference(3,'#c0392b');reference(6,'#13603f');const slot=innerWidth/count,maxLevels=Math.max(...data.map(row=>row.levels.length)),barWidth=Math.max(5,Math.min(24,slot*.72/maxLevels));data.forEach((row,index)=>{const center=left+(index+.5)*slot,bar=(sigma:number,level:any,offset:number)=>{if(sigma==null)return;const color=deps.zone(sigma).c,x=center+offset-barWidth/2,position=y(sigma),barHeight=y(0)-position;ctx.fillStyle=color;ctx.fillRect(x,position,barWidth,barHeight);ctx.strokeStyle='rgba(0,0,0,.18)';ctx.lineWidth=1;ctx.strokeRect(x,position,barWidth,barHeight);ctx.fillStyle='#16211f';ctx.font=deps.font('bold','type-caption',11.5);ctx.textAlign='center';ctx.fillText(sigma.toFixed(2),x+barWidth/2,position-5);ctx.fillStyle='#fff';ctx.font=deps.font('bold','type-overline',10.5);ctx.fillText(String(level),x+barWidth/2,y(0)-4);};row.levels.forEach((level:any,index:number)=>bar(level.metric.sigma,level.level,(index-(row.levels.length-1)/2)*barWidth*1.15));ctx.save();ctx.translate(center,top+innerHeight+12);ctx.rotate(-Math.PI/4.2);ctx.fillStyle='#3a443f';ctx.font=deps.font('','type-caption',11.5);ctx.textAlign='right';ctx.fillText(row.name.length>16?row.name.slice(0,15)+'…':row.name,0,0);ctx.restore();});ctx.strokeStyle='#16211f';ctx.lineWidth=1.3;ctx.beginPath();ctx.moveTo(left,top);ctx.lineTo(left,top+innerHeight);ctx.lineTo(width-right,top+innerHeight);ctx.stroke();ctx.fillStyle='#16211f';ctx.font=deps.font('bold','type-heading-sm',16);ctx.textAlign='left';ctx.fillText('Sigma theo xét nghiệm',left,22);let legendX=left,legendY=38;[['#c0392b','<3σ'],['#dd8b1f','3–4σ'],['#3f9a55','4–6σ'],['#13603f','≥6σ']].forEach(legend=>{ctx.fillStyle=legend[0];ctx.fillRect(legendX,legendY-9,14,8);ctx.fillStyle='#16211f';ctx.font=deps.font('bold','type-caption',11.5);ctx.fillText(legend[1],legendX+18,legendY-1);legendX+=66;});ctx.fillStyle='#6b756f';ctx.font=deps.font('','type-caption',11.5);ctx.fillText('Số trong cột = mức QC',legendX+6,legendY-1);return{bytes:deps.bytes(canvas.cv.toDataURL('image/png')),dispW:width,dispH:height};};}
