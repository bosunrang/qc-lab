export function createSigmaCanvas(deps:{scale:(width:number,height:number,value:number)=>number;create:()=>HTMLCanvasElement}){return(width:number,height:number,value:number)=>{const scale=deps.scale(width,height,value),canvas=deps.create();canvas.width=Math.round(width*scale);canvas.height=Math.round(height*scale);const context=canvas.getContext('2d')!;context.scale(scale,scale);context.fillStyle='#fff';context.fillRect(0,0,width,height);return{cv:canvas,ctx:context};};}
