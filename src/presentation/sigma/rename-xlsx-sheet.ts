export function renameXlsxSheet(bytes:any,sheetName:string,deps:{escape:(value:any)=>string;bytes:(value:string)=>any;zip:(files:any[])=>any}){const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength),decode=new TextDecoder(),files:any[]=[];let offset=0;while(offset+30<=bytes.length&&view.getUint32(offset,true)===0x04034b50){const nameLength=view.getUint16(offset+26,true),extraLength=view.getUint16(offset+28,true),size=view.getUint32(offset+18,true),nameStart=offset+30,dataStart=nameStart+nameLength+extraLength,name=decode.decode(bytes.slice(nameStart,nameStart+nameLength));let data:any=bytes.slice(dataStart,dataStart+size);if(name==='xl/workbook.xml'){const xml=decode.decode(data).replace(/(<sheet name=")[^"]*(")/,'$1'+deps.escape(sheetName)+'$2');data=deps.bytes(xml);}files.push({name,data});offset=dataStart+size;}return files.length?deps.zip(files):bytes;}
