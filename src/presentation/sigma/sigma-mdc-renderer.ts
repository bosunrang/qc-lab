export function createSigmaMdcRenderer(deps:{items:(rows:any[])=>any[];canvas:(width:number,height:number,scale:number)=>any;font:(weight:string,token:string,fallback:number)=>string;zone:(sigma:number)=>{c:string};placements:(items:any[],x:(value:number)=>number,y:(value:number)=>number,ctx:any,bounds:any)=>any[];bytes:(url:string)=>Uint8Array}){return(rows:any[])=>{const items=deps.items(rows);if(!items.length)return null;const width=780,height=420,scale=6,left=58,right=22,top=50,bottom=54,innerWidth=width-left-right,innerHeight=height-top-bottom,maxX=Math.max(50,Math.max(...items.map(point=>point.x)))*1.1,maxY=100,canvas=deps.canvas(width,height,scale),ctx=canvas.ctx,x=(value:number)=>left+Math.min(value,maxX)/maxX*innerWidth,y=(value:number)=>top+innerHeight-Math.min(value,maxY)/maxY*innerHeight;ctx.font=deps.font('','type-caption',11.5);ctx.fillStyle='#9a9486';ctx.strokeStyle='#eee7d8';ctx.lineWidth=1;ctx.textAlign='right';for(let grid=0;grid<=maxY;grid+=20){const position=y(grid);ctx.beginPath();ctx.moveTo(left,position);ctx.lineTo(width-right,position);ctx.stroke();ctx.fillText(String(grid),left-6,position+4);}ctx.textAlign='center';for(let grid=0;grid<=maxX;grid+=10)ctx.fillText(String(Math.round(grid)),x(grid),top+innerHeight+16);([[2,'#c0392b'],[3,'#dd8b1f'],[4,'#b59a00'],[5,'#3f9a55'],[6,'#0e4d4a']] as [number,string][]).forEach(line=>{const sigma=line[0],color=line[1],x2=100/sigma,endX=Math.min(x2,maxX),endY=Math.max(0,100-sigma*endX);ctx.strokeStyle=color;ctx.lineWidth=1.6;ctx.beginPath();ctx.moveTo(x(0),y(100));ctx.lineTo(x(endX),y(endY));ctx.stroke();ctx.fillStyle=color;ctx.font=deps.font('bold','type-meta',12.5);ctx.textAlign='left';ctx.fillText(sigma+'σ',x2<=maxX?x(x2)+2:width-right-22,x2<=maxX?y(0)-3:y(100-sigma*maxX)-2);});items.forEach(point=>{ctx.beginPath();ctx.arc(x(point.x),y(point.y),7,0,2*Math.PI);ctx.fillStyle=deps.zone(point.sigma).c;ctx.fill();ctx.lineWidth=1.5;ctx.strokeStyle='#fff';ctx.stroke();ctx.fillStyle='#fff';ctx.font=deps.font('bold','type-overline',10.5);ctx.textAlign='center';ctx.fillText(String(point.level),x(point.x),y(point.y)+3);});ctx.fillStyle='#16211f';ctx.font=deps.font('','type-overline',10.5);ctx.textAlign='left';deps.placements(items,x,y,ctx,{left,right:width-right,top,bottom:top+innerHeight}).forEach(point=>ctx.fillText(point.label,point.x,point.y));ctx.strokeStyle='#16211f';ctx.lineWidth=1.3;ctx.beginPath();ctx.moveTo(left,top);ctx.lineTo(left,top+innerHeight);ctx.lineTo(width-right,top+innerHeight);ctx.stroke();ctx.fillStyle='#16211f';ctx.font=deps.font('bold','type-heading-sm',16);ctx.textAlign='left';ctx.fillText('Biểu đồ Quyết định Phương pháp (MDC) — các mức QC',left,22);ctx.font=deps.font('','type-caption',11.5);ctx.fillStyle='#6b756f';ctx.fillText('Màu điểm theo xếp loại Sigma · số trong điểm là mức QC · đường 2σ–6σ',left,40);ctx.font=deps.font('','type-meta',12.5);ctx.fillStyle='#16211f';ctx.textAlign='center';ctx.fillText('CV / TEa (%)',(left+width-right)/2,height-8);ctx.save();ctx.translate(16,(top+innerHeight)/2);ctx.rotate(-Math.PI/2);ctx.fillText('|Bias| / TEa (%)',0,0);ctx.restore();return{bytes:deps.bytes(canvas.cv.toDataURL('image/png')),dispW:width,dispH:height};};}
