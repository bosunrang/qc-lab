export function createDashboardEmptyTestsHtml({emptyState,button}:{emptyState:(title:string,detail:string,action:string)=>string,button:(label:string,action:string|{action:string;args?:unknown[]}|null,variant:string)=>string}){return(isAdmin:boolean)=>emptyState('Chưa có xét nghiệm đang vận hành','Cần đưa xét nghiệm vào Panel QC, ghép Nhóm lô QC và gán Mean/SD trước khi theo dõi.',isAdmin?button('Cấu hình Mean/SD',{ action: 'goManageTargets' },'teal'):'');}
