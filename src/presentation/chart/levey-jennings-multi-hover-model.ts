export function createLeveyJenningsMultiHoverModel(deps:{date:(value:any)=>string;escape:(value:any)=>string;pointValue:(point:any,test:any)=>string;number:(value:number)=>string}){return(input:{point:any;test:any;view:any;z:number;rules:string[]})=>{const point=input.point,unit=input.test&&input.test.unit?' '+deps.escape(input.test.unit):'',zText=(input.z>=0?'+':'')+deps.number(input.z)+'s',rules=input.rules.length?deps.escape(input.rules.join(', ')):'Đạt';return`<b>${deps.date(point.date)} · Mức ${input.view.level}</b><div>Lần chạy: <b style="display:inline">${deps.escape(point.runId||'—')}</b></div><div>Lô: <b style="display:inline">${deps.escape(point.lot||input.view.lot||'?')}</b></div><div>Giá trị: ${deps.pointValue(point,input.test)}${unit}</div><div>Z: ${zText}</div><div class="muted">Luật: ${rules}</div>`;};}
