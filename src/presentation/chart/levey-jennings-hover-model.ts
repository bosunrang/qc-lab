export function createLeveyJenningsHoverModel(deps:{date:(value:any)=>string;escape:(value:any)=>string;pointValue:(point:any,test:any)=>string;number:(value:number)=>string}){return(input:{point:any;test:any;lot:string;levelText:string;z:number;rules:string[]})=>{const point=input.point,zText=(input.z>=0?'+':'')+deps.number(input.z)+'s',unit=input.test&&input.test.unit?' '+deps.escape(input.test.unit):'',rules=input.rules.length?deps.escape(input.rules.join(', ')):'Đạt';return`<b>${deps.date(point.date)} · ${input.levelText}</b><div>Lô: <b style="display:inline">${deps.escape(point.lot||input.lot)}</b></div><div>Giá trị: ${deps.pointValue(point,input.test)}${unit}</div><div>Z: ${zText}</div><div class="muted">Luật: ${rules}</div>`;};}
