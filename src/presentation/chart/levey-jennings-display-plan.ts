export function createLeveyJenningsDisplayPlan(sampleIndices:(input:{length:number;maxPoints:number;valueAt:(index:number)=>any;preserve:number[]})=>number[]){return(points:any[],ruleResults:any[],width:number)=>{const preserve:number[]=[];ruleResults.forEach((result,index)=>{if(result&&result.rules&&result.rules.length)preserve.push(index);});return sampleIndices({length:points.length,maxPoints:Math.max(240,Math.floor(width/2)),valueAt:index=>points[index]&&points[index].val,preserve});};}
