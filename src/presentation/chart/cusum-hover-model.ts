export function createCusumHoverModel(deps:{date:(value:any)=>string;number:(value:any,decimals?:number)=>string}){return(input:{point:any;cPos:any;cNeg:any;rejected:boolean})=>`<b>${deps.date(input.point.date)}</b><div>CUSUM+: ${deps.number(input.cPos,2)}</div><div>CUSUM−: ${deps.number(input.cNeg,2)}</div><div class="muted">${input.rejected?'Vượt ngưỡng h — nghi ngờ trôi/shift':'Trong tầm kiểm soát'}</div>`;}
