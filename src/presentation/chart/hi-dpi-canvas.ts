export function createHiDpiCanvasSetup(devicePixelRatio:()=>number){return(canvas:any)=>{if(!canvas.dataset.baseW){canvas.dataset.baseW=canvas.getAttribute('width')||canvas.width;canvas.dataset.baseH=canvas.getAttribute('height')||canvas.height;}const baseWidth=parseFloat(canvas.dataset.baseW)||1400,baseHeight=parseFloat(canvas.dataset.baseH)||430,cssWidth=Math.max(760,Math.round(canvas.clientWidth||baseWidth)),cssHeight=Math.round(cssWidth*baseHeight/baseWidth),renderScale=Math.min(2,Math.max(1,Number(canvas.dataset.renderScale)||1)),ratio=Math.min(3,Math.max(renderScale,devicePixelRatio()||1)),pixelWidth=Math.round(cssWidth*ratio),pixelHeight=Math.round(cssHeight*ratio);if(canvas.width!==pixelWidth||canvas.height!==pixelHeight){canvas.width=pixelWidth;canvas.height=pixelHeight;canvas.style.height=cssHeight+'px';}const context=canvas.getContext('2d');context.setTransform(ratio,0,0,ratio,0,0);return{ctx:context,W:cssWidth,H:cssHeight};};}
