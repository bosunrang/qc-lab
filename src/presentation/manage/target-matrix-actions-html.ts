export function targetMatrixActionsHtml(clearButtonHtml: string, selectButtonHtml: string, saveButtonHtml: string) {
  return `<div class="modal-f target-actions">${clearButtonHtml}${selectButtonHtml}${saveButtonHtml}</div>`;
}
