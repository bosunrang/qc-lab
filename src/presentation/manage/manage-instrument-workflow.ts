export function createManageInstrumentWorkflow(deps: { removal: (state: any, input: { id: unknown }) => any; removalDialog: (name: unknown) => any }) { return Object.freeze({removal:deps.removal,removalDialog:deps.removalDialog}); }
export function manageInstrumentRemovalDialog(name: unknown) { return {kicker:'Thao tác không thể hoàn tác',title:'Xóa máy xét nghiệm',message:`Xóa máy ${name}?`,confirmLabel:'Xóa máy',cancelLabel:'Hủy'}; }
