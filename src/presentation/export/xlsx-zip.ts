export function createXlsxZip(bytes:(value:string)=>Uint8Array){const crcTable=(()=>{const table:number[]=[];for(let value=0;value<256;value++){let crc=value;for(let bit=0;bit<8;bit++)crc=(crc&1)?(0xEDB88320^(crc>>>1)):(crc>>>1);table[value]=crc>>>0;}return table;})(),crc32=(buffer:Uint8Array)=>{let crc=0xFFFFFFFF;for(let index=0;index<buffer.length;index++)crc=crcTable[(crc^buffer[index])&255]^(crc>>>8);return(crc^0xFFFFFFFF)>>>0;};return(files:{name:string;data:Uint8Array}[])=>{const parts:Uint8Array[]=[],central:{nameB:Uint8Array;crc:number;len:number;off:number}[]=[];let offset=0;const number=(value:number,length:number)=>{const out=new Uint8Array(length);for(let index=0;index<length;index++){out[index]=value&255;value>>>=8;}return out;},push=(value:Uint8Array)=>{parts.push(value);offset+=value.length;};files.forEach(file=>{const nameB=bytes(file.name),crc=crc32(file.data),off=offset;[number(0x04034b50,4),number(20,2),number(0,2),number(0,2),number(0,2),number(0,2),number(crc,4),number(file.data.length,4),number(file.data.length,4),number(nameB.length,2),number(0,2),nameB,file.data].forEach(push);central.push({nameB,crc,len:file.data.length,off});});const centralStart=offset;central.forEach(item=>[number(0x02014b50,4),number(20,2),number(20,2),number(0,2),number(0,2),number(0,2),number(0,2),number(item.crc,4),number(item.len,4),number(item.len,4),number(item.nameB.length,2),number(0,2),number(0,2),number(0,2),number(0,2),number(0,4),number(item.off,4),item.nameB].forEach(push));const centralLength=offset-centralStart;[number(0x06054b50,4),number(0,2),number(0,2),number(central.length,2),number(central.length,2),number(centralLength,4),number(centralStart,4),number(0,2)].forEach(push);const out=new Uint8Array(parts.reduce((total,part)=>total+part.length,0));let position=0;parts.forEach(part=>{out.set(part,position);position+=part.length;});return out;};}
