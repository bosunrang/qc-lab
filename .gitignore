# Generated test and tooling output
dist/
# Đầu ra của `npm run build` (package.json's `main` trỏ vào đây).
app-dist/
.cache/
.tmp-edits/
*.log
*.tmp
*.bak
*.orig
*.rej
# Local dependency/install artifacts
node_modules/

# Editor and operating-system metadata
.DS_Store
Thumbs.db
*~

# Bản sao lồng của chính repo này (71 MB, cùng HEAD) — do thao tác sao chép
# nhầm, KHÔNG phải nguồn. Xoá được an toàn; bỏ qua để `git add -A` không nuốt
# nó vào thành submodule.
/QC Lab/

# Biên bản thẩm định do `npm run validate` sinh ra
validation-output/
