'use strict';
const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-instrument-workflow.ts')).href,program=`import { createManageInstrumentWorkflow,manageInstrumentRemovalDialog } from ${JSON.stringify(source)};const f=createManageInstrumentWorkflow({removal:(_,x)=>({record:{name:x.id}}),removalDialog:manageInstrumentRemovalDialog});console.log(JSON.stringify(f.removalDialog('M')));`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.equal(JSON.parse(r.stdout).title,'Xóa máy xét nghiệm');console.log('Manage instrument workflow TypeScript tests passed');
