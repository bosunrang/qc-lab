'use strict';
const assert=require('node:assert/strict');const{spawnSync}=require('node:child_process');const path=require('node:path');const{pathToFileURL}=require('node:url');
const source=pathToFileURL(path.join(__dirname,'..','src','presentation','dashboard','dashboard-missing-target-items.ts')).href;
const program=`import { dashboardMissingTargetItems } from ${JSON.stringify(source)};const t1={id:'T1'},t2={id:'T2'},rows=dashboardMissingTargetItems([{t:t1},{t:t2}],test=>test===t1?[{level:1},{level:2}]:[]);if(rows.length!==2||rows[0].t!==t1||rows[1].l.level!==2)throw new Error('must preserve missing-target item collection');console.log('Dashboard missing target items TypeScript tests passed');`;
const result=spawnSync(process.execPath,['--no-warnings','--input-type=module','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(result.status,0,result.stderr||result.stdout||'không thể chạy dashboard missing target items TypeScript');console.log(result.stdout.trim());
