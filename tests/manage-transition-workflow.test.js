'use strict';const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-transition-workflow.ts')).href,program=`import {manageTransitionRemovalDialog} from ${JSON.stringify(source)};console.log(JSON.stringify(manageTransitionRemovalDialog()))`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.equal(JSON.parse(r.stdout).title,'Xóa dòng chuyển tiếp lô');console.log('Manage transition workflow TypeScript tests passed');
