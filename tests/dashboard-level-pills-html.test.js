'use strict';
const assert=require('node:assert/strict');const{spawnSync}=require('node:child_process');const path=require('node:path');const{pathToFileURL}=require('node:url');
const source=pathToFileURL(path.join(__dirname,'..','src','presentation','dashboard','dashboard-level-pills-html.ts')).href;
const program=`import { createDashboardLevelPillsHtml } from ${JSON.stringify(source)};const html=createDashboardLevelPillsHtml({targetOk:level=>level.id==='a',render:item=>item.level.id+':'+item.today+':'+item.targetOk+':'+item.cv})([{l:{id:'a'},todayLevel:true,st:{cv:2}},{l:{id:'b'},todayLevel:false,st:null}]);if(html!=='a:true:true:2b:false:false:null')throw new Error('must preserve dashboard level pills');console.log('Dashboard level pills HTML TypeScript tests passed');`;
const result=spawnSync(process.execPath,['--no-warnings','--input-type=module','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(result.status,0,result.stderr||result.stdout||'không thể chạy dashboard level pills HTML TypeScript');console.log(result.stdout.trim());
