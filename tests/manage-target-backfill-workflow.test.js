'use strict';const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-target-backfill-workflow.ts')).href,program=`import {createManageTargetBackfillWorkflow} from ${JSON.stringify(source)};console.log(JSON.stringify(createManageTargetBackfillWorkflow({pointsForPick:p=>[p.id]}).points([{id:'A',use:true},{id:'B',use:false}])))`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.deepEqual(JSON.parse(r.stdout),['A']);console.log('Manage target backfill workflow TypeScript tests passed');
