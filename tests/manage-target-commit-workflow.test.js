'use strict';const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-target-commit-workflow.ts')).href,program=`import {manageTargetCommitAudit} from ${JSON.stringify(source)};console.log(manageTargetCommitAudit({panel:'P',group:'G',count:2,mode:'planned'}))`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.equal(r.stdout.trim(),'P · G · 2 dòng (dự kiến)');console.log('Manage target commit workflow TypeScript tests passed');
