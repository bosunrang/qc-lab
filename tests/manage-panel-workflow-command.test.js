const assert=require('node:assert/strict');
const {spawnSync}=require('node:child_process');
const path=require('node:path');
const program=`import {createManagePanelWorkflowCommand} from './src/application/manage/manage-panel-workflow-command.ts';let logs=[],saves=[],closed=0,renders=0;const command=createManagePanelWorkflowCommand({current:()=>({}),panel:{save:input=>({ok:true,effects:{audit:{action:'Thêm',detail:input.data.name,target:'Panel'},save:{clearDerived:false}}}),remove:()=>({ok:true,effects:{audit:{action:'Xóa',detail:'P1',target:'Panel'},save:{clearDerived:false}}})},log:(...item)=>logs.push(item),saveState:value=>saves.push(value),close:()=>closed++,render:()=>renders++});const saved=command.save({newId:'p1',data:{name:'P1'}}),removed=command.remove({id:'p1'});console.log(JSON.stringify({saved,removed,logs,saves,closed,renders}));`;
const output=spawnSync(process.execPath,['--experimental-strip-types','--input-type=module','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(output.status,0,output.stderr);const value=JSON.parse(output.stdout);assert.equal(value.saved.ok,true);assert.equal(value.removed.ok,true);assert.deepEqual(value.logs.map(item=>item[0]),['Thêm','Xóa']);assert.equal(value.closed,1);assert.equal(value.renders,2);console.log('Manage panel workflow command TypeScript tests passed');
