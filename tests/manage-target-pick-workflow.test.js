'use strict';const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-target-pick-workflow.ts')).href,program=`import {createManageTargetPickWorkflow} from ${JSON.stringify(source)};const f=createManageTargetPickWorkflow({normalize:x=>({mean:+x.meanRaw})});console.log(JSON.stringify([f.readRow({testId:'T',lot:'L',use:false,values:{}}),f.readRow({testId:'T',lot:'L',use:true,values:{meanRaw:'2'}})]))`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.equal(JSON.parse(r.stdout)[1].mean,2);console.log('Manage target pick workflow TypeScript tests passed');
