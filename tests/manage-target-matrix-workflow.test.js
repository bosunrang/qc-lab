'use strict';const assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),{pathToFileURL}=require('node:url'),path=require('node:path');const source=pathToFileURL(path.join(__dirname,'..','src','presentation','manage','manage-target-matrix-workflow.ts')).href,program=`import {createManageTargetMatrixWorkflow} from ${JSON.stringify(source)};const f=createManageTargetMatrixWorkflow();console.log(JSON.stringify([f.lockedBackfillDialog(2,'T8'),f.reauth(false),f.reauth(true)]))`,r=spawnSync(process.execPath,['--no-warnings','--input-type=module','--experimental-strip-types','--eval',program],{cwd:path.join(__dirname,'..'),encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.equal(JSON.parse(r.stdout)[0].title,'Điền lô/Mean-SD cho điểm QC đã khóa kỳ');console.log('Manage target matrix workflow TypeScript tests passed');
